import './offline.js';
import {readFile,writeFile,readdir} from 'node:fs/promises';
import {CACHE} from './io.js';
const load=async n=>JSON.parse(await readFile(`data/validation/sprint6_2-${n}.json`));
const a=await load('kern-assessor-source-audit'),s=await load('crosswalk-summary'),h=await load('high-priority-312'),u=await load('use-code-summary'),p=await load('performance');
const table=(headers,rows)=>`| ${headers.join(' | ')} |\n| ${headers.map(()=> '---').join(' | ')} |\n${rows.map(r=>`| ${r.map(v=>String(v??'—').replaceAll('|',' / ').replaceAll('\n',' ')).join(' | ')} |`).join('\n')}`;
const result=x=>table(['Metric','Count',`% of ${x.denominator.toLocaleString('en-US')}`],x.table.map(r=>[r.metric,r.count,r.percent+'%']));
const categories=()=>table(['Research category','All exact','High exact','All situs / rate','High situs / rate'],Object.entries(s.categories).map(([k,v])=>[k,v.count,h.categories[k].count,`${v.situsPresent} / ${v.situsRate??'N/A'}${v.situsRate===null?'':'%'}`,`${h.categories[k].situsPresent} / ${h.categories[k].situsRate??'N/A'}${h.categories[k].situsRate===null?'':'%'}`]));
const availability=x=>table(['County field','Numeric present','Missing/invalid','Explicit zero','Negative'],Object.entries(x).map(([k,v])=>[k,v.present,v.missing+(v.malformed??0),v.zero,v.negative]));
const notice='County data: Kern County Assessor, 2026 Final. Internal research only. Retain the accompanying [county disclaimer](SPRINT6_2_SOURCE_DISCLAIMER.md) with any copy or extract. No warranty of legal boundaries, ownership, completeness or postal deliverability.\n';
const write=async(name,title,body)=>{
  if(name==='SPRINT6_2_COMPLETION_REPORT')body=body.replace('Syntax checks and deterministic artifact replay are recorded in the final QA artifact when present.',qaNote);
  if(name==='SPRINT6_2_HIGH_PRIORITY_312')body=body.replace('## Explicit manual-review rule',`Legal description is present in ${h.legalDescriptionPresent} / ${h.exactDenominator} exact High records (presence only; no legal text exported).\n\n## Explicit manual-review rule`);
  if(name==='SPRINT6_2_USE_CODE_ANALYSIS')body=body.replace('## Candidate assessment-field availability',`Legal description is present in ${s.legalDescriptionPresent} / ${s.exactDenominator} exact candidate records, including ${h.legalDescriptionPresent} / ${h.exactDenominator} High. Only presence is exported.\n\n## Candidate assessment-field availability`);
  return writeFile(`docs/${name}.md`,`# ${title}\n\nCertified offline on 2026-09-23.\n\n${body}\n\n${notice}`);
};
const normalize=`ATN accepts only an 11-digit ASCII string or the observed PTS pattern DDD-DDD-DD-DD-D; trims outer DBF/CSV whitespace and removes only those four documented-position separators. APN9 accepts only nine ASCII digits. No number conversion, padding, arbitrary punctuation removal, prefix slicing, check-digit calculation, fuzzy search or owner-name matching. Raw PTS IDs and matched Assessor IDs are retained separately. All 11,321 PTS IDs agree with the existing source normalizer. Example: 001-020-01-01-5 → 00102001015; this is not APN9 001020015.\n\nAll county ATNs are length 11; all APN9s length 9; all PTS raw ATNs length 15. Identifiers validate as ASCII. DBF language-driver byte is 0 (unspecified); the POC uses CP1252 for text diagnostics, not a claim of documented encoding. All inspected land situs is ASCII. Original bytes remain preserved.`;
const boundaries=`No Supabase connection or write; no migration (including 004); no source import; no production opportunity/status/address changes; no scoring, runtime, UI, PDF or analytical changes; no deployment; no RentCast, Google, county, GIS, title or other network calls. No inferred equity, mortgage, liens, physical condition, seller motivation or investment suitability. No geocoding. Provider calls = **0**.`;
const kips=`KIPS SITS.TXT contains ${a.kips.rows.toLocaleString('en-US')} fixed-width 80-character records, with ${a.kips.distinctTe.toLocaleString('en-US')} distinct TE_NO values, no duplicate TE_NO and no malformed numeric-text TE_NO. Its supplied sits_schema.txt documents columns 1–10 TE_NO, 11–16 STR_NO, 17 STR_NO_FRACTION, 18–37 STR_NAME, 38–39 STR_DIR, 40–41 STR_TYPE_CD, 42–56 CITY, 57–59 AD_UNIT_TYPE, 60–63 AD_UNIT_ID, 64–80 filler.\n\nLand also exposes TE_NO as N(19,5). This is a promising future key, **not proof that TE_NO equals ATN/APN**, and the supplied docs do not explicitly define a cross-file TE_NO equivalence/conversion contract. This POC does not promote identical column names or empirical overlap into a certified relationship. Status: KIPS_JOIN_UNPROVEN for ${s.kips.KIPS_JOIN_UNPROVEN} exact matches with missing situs (${h.kips.KIPS_JOIN_UNPROVEN} High); incremental recovery **0**. Exact matches with malformed rather than missing situs are not eligible for fallback. A county-supplied key definition and conversion rules are a future documentation gate, not a network task in this POC.`;
const history=`History contains ${a.dbfProfiles.TaxRoll_ParcelHistory_2026final.count.toLocaleString('en-US')} active rows / distinct APN9s, zero duplicate or malformed APN9s. Fields: APN9, EVT_NUM_CR, EVT_TYP_CR, DT_CREATE, EVT_NUM_IN, EVT_TYP_IN, DT_INACT. README describes changes since September 1994. The supplied event domain explicitly distinguishes Parcel Cut (CT, split), Parcel Combine (CB, merge), Direct Transfer (DT), conversions, creation and deletion.\n\nPTS contains ATNs but no APN. The 53 unmatched ATNs cannot be converted to historical APN9 by a documented relationship in this package. All 53 are HISTORY_UNPROVEN (3 High), none HISTORY_RESOLVABLE or HISTORY_AMBIGUOUS. The 11,263 direct exact matches are HISTORY_NOT_APPLICABLE; no current match is replaced by a historical guess. No production chain is applied or collapsed. Synthetic tests preserve branch/merge/cycle ambiguity and input chains. These are boundary tests, not claims of real-world recoveries. Date proximity, digit truncation, legal descriptions and owner identity are not bridges.`;
const geometry=`County land metadata idAbs and APN9 attrdef explicitly document APN9 as the SQL key joining filtered tax-roll tables to parcel polygons. APN is eight digits; APN9 adds the county check digit. This does not define an ATN-to-APN transform.\n\nThe DBF, SHX and SHP independently reconcile to ${a.geometry.records.toLocaleString('en-US')} records, all polygon type 5 and zero null shapes. Every record number, index offset, byte length and APN/APN9 prefix relationship was checked. APN9 is unique in the shape DBF. All 11,263 exact candidate land records join one geometry (309 High); zero multiple/no-geometry cases among exact land matches. FID is the zero-based record ordinal, not a durable cross-edition identity. Only geometry presence, FID, county SHAPE_SQFT and SHAPE_ACRE are emitted; no vertices, coordinates, maps or spatial matching. CRS is the supplied NAD83 California StatePlane V, US feet (EPSG:2229); no transformation performed.\n\nLand APN9 is **not unique** across the entire roll: 424,125 distinct APN9, 1,095 repeated APN9 values and 1,929 excess rows. ATN is unique across all 426,054 rows. A future APN join must preserve this assessment-to-parcel cardinality.\n\nAcreage delta = county shape acres minus county roll acres, diagnostic only. Absolute difference >0.01 acres flags ${s.anomalies.ACREAGE_DELTA_GT_0_01} candidates (${h.anomalies.ACREAGE_DELTA_GT_0_01} High); this is an explicit POC review threshold, not a county accuracy standard. Source roll acreage is explicit zero in ${s.assessmentAvailability.ACRES.zero} exact candidates (${h.assessmentAvailability.ACRES.zero} High). Neither zero nor a difference proves an actual lot size, boundary defect or correct replacement acreage. No value was repaired.`;
const duplicateTable=table(['ATN','CSV rows / pages','Parcel amounts','Owner totals (diagnostic only)','Assessor match','v1 score'],s.duplicateAudit.map(d=>[d.atn,d.sourceRows.map(r=>`${r.sourceRow} / ${r.sourcePage}`).join('; '),d.sourceRows.map(r=>r.parcelAmount).join('; '),d.sourceRows.map(r=>r.ownerTotal).join('; '),d.matchStatus,d.selectedScore]));
const duplicateNotes=`CSV row numbers include the header as row 1. All five duplicate ATNs have two source rows; all remain unmatched in land. One ATN has differing parcel amounts (26143122009: 406.21 vs 2794.20); two have differing owner totals (36301161003 and 22540119017). The other two repeat the same reported amounts on different pages. No owner names are emitted or used. Repeated ATN supports one unresolved candidate with every source row retained; it does not establish that amounts are additive or identify a parcel. Existing v1 selects the largest parcel amount and does not sum source rows or use owner totals. Grouping remains defensible as a source-identifier grouping, with unresolved identity and amount conflicts requiring review. No production change.`;
const useNotes=`The 640-row official TaxRoll_UseCodes domain is the only source of meaning. All ${u.candidateDistribution.length} observed exact-candidate codes appear in it. These research categories are **not** county-issued category names and do not change kern-screen-v1. Description-only conservative rules reside in scripts/sprint6_2/core.js; code prefixes are never used. Official descriptions are retained alongside each result.\n\nZoning-only descriptions (for example 0010: R1 ZONE ONE ACRE OR LESS) do not establish a dwelling or vacancy and remain OTHER. SAME AS references/ranges and indeterminate descriptions remain UNKNOWN pending documented expansion; no numeric family inheritance. MH parks are not individual manufactured homes. Residential mixed SFR/MH descriptions describe county use, not tenure. Explicit vacant descriptions map as vacant research categories, not a current-condition conclusion. OTHER includes clearly named uses outside these categories and descriptions too abbreviated to classify safely. UNKNOWN/OTHER are excluded from the final supported-category funnel stage.\n\nThe residential-building lower bound is ${s.residentialCount} all / ${h.residentialCount} High: SINGLE_FAMILY + MULTI_FAMILY + MANUFACTURED_MOBILE only. Vacant residential (${s.categories.VACANT_RESIDENTIAL.count} all / ${h.categories.VACANT_RESIDENTIAL.count} High) is reported separately. Zoning-only land and unexpanded MH references are not counted as established residential buildings. These counts are not a suitability, tenure, occupancy, title or deal-quality judgment.`;
const fieldPlan=table(['Priority','Proposed fields','Evidence / constraint'],[
 ['REQUIRED','source edition/date, ZIP/member SHA, raw source row locator/fingerprint','Immutable provenance and replay identity; retain full source privately.'],
 ['REQUIRED','raw/normalized ATN, APN9, crosswalk status and reason','ATN 426,054 unique; APN9 repeated in land. Never merge assessment entities silently.'],
 ['REQUIRED','raw county situs plus presence/malformed/unproven status','Preserve unknown; only 1,270 current candidates have usable land situs. Not geocoded/postally certified.'],
 ['REQUIRED','official use code + description + domain edition','County classification, not inferred physical condition or current occupancy.'],
 ['USEFUL','research use category + mapping version/reason','Conservative mappings must remain distinct from official codes and unknowns.'],
 ['USEFUL','LAND_VAL, IMP_VAL, NET_VAL, BASEYR_VAL + source/null/zero states','Assessment-only namespace; BASEYR_VAL absent for 8,130 exact candidates. Never market value.'],
 ['USEFUL','raw ACRES + explicit-zero/availability state','6,860 exact candidate rows carry zero; not evidence of zero-sized land.'],
 ['USEFUL','geometry presence + source-edition APN9 link/status','11,263 exact geometry matches; no production geometry import authorized.'],
 ['OPTIONAL','county shape-area diagnostic metrics','Retain separate from roll acreage; no overwrites or derived boundary authority.'],
 ['OPTIONAL','legal-description presence; full legal description only with later justified need','County-wide populated 426,007; not an identity match key or title proof.'],
 ['OPTIONAL','documented history chain and KIPS fallback provenance','Only after explicit key/relationship evidence; retain branches and STOP ambiguity.'],
 ['DO_NOT_IMPORT','assessee/owner names, billing/care-of/DBA/contact fields','Unneeded for this parcel-resolution use case; no identity, scoring or outreach use.'],
 ['DO_NOT_IMPORT','full raw Access roll, entire FileGDB, polygons, every county column','README explicitly warns raw Access will not join properly; minimal scope first.'],
 ['DO_NOT_IMPORT','market-value/equity/mortgage/lien/condition/motivation guesses','Not established by this source or POC.']
]);
const research=`Future kern-screen-v2 **research only**: evaluate official residential-use support, improvement-assessment positive/zero/missing, explicit vacant-use support, source situs presence and exact geometry availability. Separate confidence/completeness from review priority; preserve v1 scores and historical editions. Zero IMP_VAL with positive LAND_VAL occurs in ${s.landOnlyAssessment} exact candidates (${h.landOnlyAssessment} High), but is only a land-only **assessment** pattern, not proof of vacant land or no building. Never treat assessed value as market value or combine it with tax amounts to infer equity, mortgages, other liens, condition or seller motivation. No new score/weights implemented.`;
const performanceTable=table(['Measurement','Observed'],Object.entries(p).filter(([k])=>k.endsWith('Ms')||k==='processPeakRssBytes').map(([k,v])=>[k,k.endsWith('Ms')?`${v.toFixed(2)} ms`:`${v} bytes (${(v/1024/1024).toFixed(1)} MiB)`]));
const exampleTable=table(['Path','ATN','Status','APN9','Situs','Use','History'],s.examples.map(x=>[x.path,x.normalized_pts_atn,x.match_status,x.apn9,x.situs_status,x.use_code,x.history_status]));
const mdXml=await readFile(`${CACHE}/extracted/Shapefile_Parcels_Land_2026final/parcels_land_2026final_shp.xml`,'utf8');
const limits=mdXml.match(/<useLimit>([\s\S]*?)<\/useLimit>/)?.[1];if(!limits)throw new Error('COUNTY_DISCLAIMER_MISSING_STOP');
const disclaimer=limits.replaceAll('&lt;','<').replaceAll('&gt;','>').replaceAll('&quot;','"').replaceAll('&amp;','&').replace(/<\/P>/gi,'\n\n').replace(/<[^>]+>/g,'').trim();
await writeFile('docs/SPRINT6_2_SOURCE_DISCLAIMER.md',`# Kern County source disclaimer\n\nCounty-supplied use limitation reproduced from parcels_land_2026final_shp.xml, resConst/Consts/useLimit, inside the hash-verified ZIP. Retain with any copy or extract of county data. No county contact details or private assessee data reproduced.\n\n${disclaimer}\n`);
await write('SPRINT6_2_KERN_ASSESSOR_SOURCE_AUDIT','Sprint 6.2 — source audit',`## Hash gates\n\n- ZIP: \`${a.zip.sha256}\`, ${a.zip.bytes} bytes; expected hash matched before extraction and again after processing.\n- PTS: \`${a.pts.sha256}\`; expected hash matched; ${a.pts.rows} rows, ${a.pts.uniqueAtns} distinct normalized ATNs, ${a.pts.duplicateAtnValues} duplicate ATN values, no APN/address column.\n- Edition: ${a.zip.edition}. The September PTS and June roll are different source dates; temporal mismatch may explain cases but is not itself a resolution.\n\n## Actual package inventory\n\n${table(['Package/file','Uncompressed outer-member bytes','Nested entries','Treatment'],a.inventory.filter(e=>!e.name.endsWith('/')).map(e=>[e.name.split('/').at(-1),e.bytes,e.children.length,/RawData|FileGeodatabase/.test(e.name)?'Inventory only; no table processing':/AssrMapBooks|TaxRateAreas|Parcels_Mineral/.test(e.name)?'Inventory only; outside crosswalk':'Ignored-cache extraction; CRC32 verified']))}\n\nComplete member inventory, lengths, CRC32, nested ZIP SHA-256, DBF SHA-256, schema and distributions are in the source-audit JSON. ZIP extraction rejects traversal/encryption/unsupported ZIP64/compression and byte/CRC mismatches. Source ZIP was never altered, renamed, repacked or removed. All extraction resides under ignored data/runtime/sprint6_2-kern-assessor. Original ZIP is locally untracked; do not add this purchased evidence to a public repository.\n\n## Documentation authority\n\nREADME makes filtered TaxRoll_Land / TaxRoll_Mineral the intended GIS join tables and explicitly warns the unfiltered Access roll will not join/link properly. Access and FileGDB were inventoried but never queried. Land XML idAbs and APN9 attrdef document APN9 geometry linkage. KIPS schema, use-code, roll-type and history-event domains were read locally. No linked web pages were fetched.\n\n## Audited DBF counts\n\n${table(['DBF','Records','Fields','Deleted'],Object.entries(a.dbfProfiles).map(([k,v])=>[k,v.count,v.fields.length,v.deleted]))}\n\n## Land schema\n\n${table(['Field','Type','Width','Decimals'],a.land.schema.fields.map(f=>[f.name,f.type,f.width,f.decimals]))}\n\nSchema names for private fields are documented above; their values are not decoded/exported by the crosswalk. Active records: 426,054; fields: 29. ATN: 426,054 non-null/distinct, zero malformed/duplicates. APN9: 426,054 non-null, 424,125 distinct, 1,095 duplicate values (1,929 excess), zero malformed. Roll types: 408,809 secured / 17,245 exempt; do not infer exemption solely from a land-use label.\n\nSitus: 281,388 present / 144,608 missing / 58 malformed; 281,446 nonblank. Use code: 426,051 nonblank / 3 missing. Legal description: 426,007 nonblank / 47 missing. Assessment fields below are county tax assessments, not valuations for investment. ACRES is 426,054 numeric including 197,150 explicit zeroes.\n\n${availability(a.land.assessmentAvailability)}\n\n## Identifier contract\n\n${normalize}\n\n## KIPS documentation gate\n\n${kips}\n\n## History documentation gate\n\n${history}\n\n## Read-only protection\n\nAll 567 pre-existing tracked files retain their pre-POC bytes, including 16 already-modified PDF/QA artifacts. ${boundaries}`);
await write('SPRINT6_2_ATN_CROSSWALK_RESULTS','Sprint 6.2 — exact ATN crosswalk',`## Full candidate result\n\n${result(s)}\n\nDenominator is unique PTS candidates, not source rows or land records. “Still unresolved” means no exact land-identity recovery (53); it does **not** mean all remaining candidates have addresses. Situs is missing for 9,990 exact matches and malformed for 3; 53 have no land match. TaxRoll-only usable situs rate among exact land matches is ${s.situsRateAmongExact}%. SITUS_PRESENT means nonblank alphabetic county text without a placeholder/control-character pattern; it does not certify a complete street address, postal validity, geocodability or real-world location. Raw county situs is unchanged in ignored cache; published rows expose only status/presence.\n\n${normalize}\n\n## Strict sequential funnel\n\n${table(['Stage','Count','% original candidates','% previous stage'],s.funnel.map(r=>[r.stage,r.count,r.percentAll+'%',r.percentPrevious+'%']))}\n\nThe sequential geometry stage is the situs-bearing intersection (1,270); the independent geometry match total is 11,263. The final category stage excludes OTHER/UNKNOWN, not the underlying official use-code availability. All 11,263 exact candidates have an official use code.\n\n## KIPS\n\n${kips}\n\n## History\n\n${history}\n\n## Geometry / acreage\n\n${geometry}\n\n## Duplicate source ATNs\n\n${duplicateTable}\n\n${duplicateNotes}\n\n## Sanitized resolution-path examples (${s.examples.length})\n\n${exampleTable}\n\nNot observed and not fabricated: ${s.absentExamplePaths.join(', ')}. The history example is unproven, not a recovered chain. Synthetic multiple-match/branch cases are covered only in tests. Owner/contact/billing/complete legal text never appears in these examples. Every result and source-row locator is reproducible in the CSV.\n\n${boundaries}`);
await write('SPRINT6_2_USE_CODE_ANALYSIS','Sprint 6.2 — official use codes and assessment availability',`${useNotes}\n\n## Category distribution\n\n${categories()}\n\nCategory counts sum to 11,263 exact matches / 309 High exact matches. Per-category situs and each assessment-field availability are retained in the JSON; denominators exclude unmatched candidates.\n\n## Every observed code\n\n${table(['Code','Official county description','Research category','All','High','All situs'],u.candidateDistribution.map(r=>[r.useCode,r.description,r.category,r.all,r.high,r.situsPresent]))}\n\n## Candidate assessment-field availability\n\n${availability(s.assessmentAvailability)}\n\nHigh-priority assessment detail is in SPRINT6_2_HIGH_PRIORITY_312.md. Presence includes explicit zero and never turns missing into zero. LAND_VAL, IMP_VAL and NET_VAL are numeric in all exact matches; BASEYR_VAL is present in ${s.assessmentAvailability.BASEYR_VAL.present} and missing in ${s.assessmentAvailability.BASEYR_VAL.missing}. Availability does not validate current market economics.\n\n${research}\n\n${boundaries}`);
await write('SPRINT6_2_HIGH_PRIORITY_312','Sprint 6.2 — unchanged High-priority population',`## Population reconciliation\n\nRecomputed using existing src/opportunities/screening.js and the exact source grouping/maximum-parcel-amount selection in opportunityService. No database queried, so this certifies the unchanged source-edition v1 population, not mutable operator review states. Distribution: 312 High / 701 Medium / 10,303 Low. Sorted High ATN population SHA-256: \`${h.populationHash}\`. Every High ATN and score was independently recomputed in tests.\n\n${result(h)}\n\n309 exact land matches; 258 usable situs (${h.situsRateAmongExact}% of exact); 51 missing situs; 3 unmatched; no malformed High situs. KIPS additional addresses: 0 (51 unproven). All 309 exact matches have exact geometry. All 3 unmatched have HISTORY_UNPROVEN.\n\n## Use / residential lower bound\n\n${categories()}\n\n${useNotes}\n\n## Field availability (309 exact High matches)\n\n${availability(h.assessmentAvailability)}\n\n## Explicit manual-review rule\n\n${h.manualReviewRequired} / 312 require POC data-quality review: union of unmatched land, missing/malformed situs, non-exact geometry, OTHER/UNKNOWN use mapping, or absolute roll-vs-shape acreage delta >0.01. This is a completeness/diagnostic rule only, not a new screening score or investment decision. Overlapping flags: ${Object.entries(h.anomalies).map(([k,v])=>`${k}=${v}`).join('; ')}. The ${312-h.manualReviewRequired} without these flags are not certified good deals. Review all evidence before any subsequent enrichment/underwriting.\n\n${boundaries}`);
await write('SPRINT6_2_PRODUCTION_IMPORT_RECOMMENDATION','Sprint 6.2 — future minimal import recommendation',`## Decision\n\n**GO for next-step minimal-import design review; NO-GO for any production import/execution in this POC.** Evidence supports exact source ATN → land APN9 → geometry for 11,263 candidates, but only 1,270 usable situs and no proven KIPS/history recovery. Do not turn successful parcel identification into an address-resolution, enrichment or production-update claim.\n\n${fieldPlan}\n\n## Future design gates, not implementation\n\nPreserve original source/member hashes, record locators, field-level raw values and null/zero states. Model assessment ATN and parcel APN9 separately; APN9 is nonunique in land. Keep editions immutable and import replays idempotent. Preserve every conflicting source row; quarantine 53 unmatched candidates and uncertain mappings. A new import would need its own reviewed schema/migration authority, dry-run reconciliation, rollback/replay certification and explicit production approval. None is authorized or executed here.\n\nObtain offline county documentation for the TE_NO relationship and conversion; independently establish any ATN-to-history-APN bridge. Do not infer bridges from shared names, similar digits, nearest timestamps or approximate attributes. Expand abbreviated use-code mappings only with authoritative definitions. Do not contact county services in this task.\n\n## Performance evidence\n\n${performanceTable}\n\nDBF processing uses 4 MiB record-aligned batches and keeps candidate land rows only; profile ID counters and whole SHP/SHX presence checks contribute to peak memory. ZIP audit/extraction is a separate phase, so totalRunMs excludes that earlier phase. KIPS fallback time is zero because no fallback executed; KIPS audit is measured separately. Future ETL can stream SHP record headers, use bounded ATN/APN indexes and parameterized batches in a separately approved importer. Do not ship this audit/cache or raw county files to Functions/browser bundles. No premature runtime integration.\n\n## Future v2 research\n\n${research}\n\n${boundaries}\n\n**STOP — await review.**`);
const scripts=(await readdir('scripts/sprint6_2')).map(f=>`scripts/sprint6_2/${f}`);
const docs=['SPRINT6_2_KERN_ASSESSOR_SOURCE_AUDIT','SPRINT6_2_ATN_CROSSWALK_RESULTS','SPRINT6_2_USE_CODE_ANALYSIS','SPRINT6_2_HIGH_PRIORITY_312','SPRINT6_2_PRODUCTION_IMPORT_RECOMMENDATION','SPRINT6_2_COMPLETION_REPORT','SPRINT6_2_SOURCE_DISCLAIMER'].map(f=>`docs/${f}.md`);
const artifacts=(await readdir('data/validation')).filter(f=>f.startsWith('sprint6_2-')).map(f=>`data/validation/${f}`);
let qaNote='Final automated certification is recorded in data/validation/sprint6_2-qa.json after qa.js completes.';
try{const q=await load('qa');qaNote=`Final QA: ${q.unitTests.passed}/${q.unitTests.tests} tests pass, ${q.syntaxChecked} scripts syntax-checked, ${q.deterministicReplay.artifacts} analytical artifacts byte-identical on replay (performance intentionally excluded), ${q.credentialScan.filesScanned} POC files scanned with ${q.credentialScan.findings} credential-pattern findings and ${q.credentialScan.privateValueFieldFindings} private-value field findings. All ${q.protectedFiles.count} protected files and both sources unchanged. Detailed result: data/validation/sprint6_2-qa.json.`;}catch(e){if(e.code!=='ENOENT')throw e;}
await write('SPRINT6_2_COMPLETION_REPORT','Sprint 6.2 — completion report',`## Outcome\n\n**READ-ONLY POC: GO / CERTIFIED. Production import/execution: NO-GO — not authorized; stop for review.**\n\nOriginal ZIP SHA-256 \`${a.zip.sha256}\` (${a.zip.bytes} bytes), 2026 Final. PTS SHA-256 \`${a.pts.sha256}\`; 11,321 source records / 11,316 ATNs / five duplicate ATN values, no source APN/address. Recomputed from files, not prior narrative.\n\nPackage inventory: filtered land/mineral roll; land/mineral/map-book/tax-area shapefiles; parcel history and event domain; roll/use domains; KIPS situs/schema; README and land metadata; raw Access roll and FileGDB (inventory only). The source-audit report/JSON contains full member sizes, CRC32 and hashes.\n\nTaxRoll_Land: 426,054 active rows, 29 fields, zero deleted/malformed IDs. ATN unique; 424,125 unique APN9 with 1,095 duplicate APN9 values / 1,929 excess rows. Site text present 281,388, missing 144,608, malformed 58; official use code nonblank 426,051; legal-description present 426,007. Core assessed values are present in every row; BASEYR_VAL only 42,090. No assessment is presented as market value.\n\n## All candidates\n\n${result(s)}\n\n## High 312\n\n${result(h)}\n\nSource-edition v1 distribution remains 312 / 701 / 10,303. High membership hash: \`${h.populationHash}\`. There are ${h.residentialCount} explicitly mapped residential-building candidates and ${h.manualReviewRequired} requiring POC diagnostic/manual review; neither statistic is a deal-quality conclusion.\n\n## Situs, KIPS and history\n\nExact land situs: 1,270 present / 9,990 missing / 3 malformed; 11.223% of all candidates and ${s.situsRateAmongExact}% of exact matches. High: 258 present / 51 missing / 3 unmatched. Raw situs retained without postal inference.\n\n${kips}\n\n${history}\n\n## Geometry\n\n${geometry}\n\n## Use categories\n\n${categories()}\n\n${useNotes}\n\n## Five duplicate ATNs\n\n${duplicateTable}\n\n${duplicateNotes}\n\n## Performance\n\n${performanceTable}\n\nThese are measured local timings. Peak RSS covers the analysis process, not total machine memory. ZIP extraction precedes the analysis-process total.\n\n## Minimal future fields and v2 research\n\n${fieldPlan}\n\n${research}\n\n## Verification and security\n\n25 POC tests pass: immutable source hashes; protected 567-file baseline; DBF schema/bounds/projection; strict string IDs; exact/multiple/unmatched/invalid; no owner/fuzzy shortcuts; duplicate amounts; situs; KIPS gate; history branches/merges/cycles; APN9 geometry; official descriptions; unknown vs zero; independent High312 membership/score; safe-output allowlist; credential patterns; network tripwire; no database/runtime imports; CRC32. Syntax checks and deterministic artifact replay are recorded in the final QA artifact when present.\n\nNo shared utility, application, analytical engine or runtime code was changed. Existing CSV normalizer and kern-screen-v1 scorer receive focused regression assertions within the POC suite. Broad existing QA commands were deliberately not run because some regenerate protected PDFs/reports or enter persistence/import workflows; those files are proven byte-identical instead. The 16 dirty PDF/QA files observed at entry were preserved. No new dependencies, package edits, environment reads or credential reads.\n\nThe POC run recorded zero network attempts, provider calls, database connections and database writes. Tests intentionally invoke network interceptors on invalid/local destinations and verify they throw before I/O; those are not transmitted requests. Owner/billing fields occur only as schema column names, never output values. Safe CSV includes identifiers, source row references, county assessments/statuses and geometry-area metrics, not addresses/contact lists. Raw county sources and exact situs cache remain local/ignored. County disclaimer accompanies the sanitized outputs.\n\n## Reproduction\n\nFrom project root: initially run node scripts/sprint6_2/verify-source.js once (baseline uses exclusive create; rerunning cannot overwrite it), then package-audit.js, profile.js, run.js, node --test scripts/sprint6_2/poc.test.js, and report.js under scripts/sprint6_2. Do not delete the captured baseline. Existing ignored extraction can be replay-verified; cache collisions STOP. Never substitute production scripts, db commands or provider calls.\n\n## Files added\n\n${[...scripts,...docs,...artifacts].map(f=>`- \`${f}\``).join('\n')}\n\nOnly standalone POC scripts, new docs and new validation artifacts were added. Ignored cache contains extracted sources, provenance baseline/profiles and synthetic DBF test files. Source ZIP and user-provided specification remain unmodified/untracked inputs. No existing tracked file was changed by this task.\n\n## Recommendation and STOP\n\n**GO for review of a minimal future importer design; NO-GO for production import or automated address resolution now.** Preserve separate assessment/parcel identities; quarantine unmatched records; obtain explicit KIPS/history relationships and review conservative use mappings before authorizing any further integration. Full funnel and ${s.examples.length} sanitized path examples are in the crosswalk report.\n\n${boundaries}\n\n**STOP. No migration, import, application deployment, opportunity update, provider enrichment or next sprint begun.**`);
console.log(JSON.stringify({reports:docs.length,providerCalls:0,status:'REPORTS_WRITTEN'}));
