# Sprint 6.3 import runbook — Production certified

The operator authorized migration 004 exactly once, after prerequisite checks, and subsequently authorized the certified production county import. Migration 004 applied on 2026-09-23 at 20:25:49.044Z; production schema certification passed. The corrected rollback-only Gate 12 runner used parameterized `set_config`, passed all synthetic rollback/replay/preservation checks and left zero rows before import. Gate 13 then committed batch `6055c2fe-20e8-427a-8912-e46a655ff7e7` (11,316 observations) and reconciled an identical replay with zero additions. The certified preview revision `6ab469b7e165d1222cdd9ad7` was deployed to production as `6ab46d05cc42f49433bf22ac`; read-only runtime, privacy, UI, performance, security and regression certification passed. **Final status: PRODUCTION CERTIFIED. Do not rerun 004 or the import.** See the completion report, import receipt and `data/validation/sprint6_3-production-certification.json`.

## Separate isolated certification authority

Run `node scripts/db-schema-certify.js --certify-sprint6-3` only for the approved isolated certification. It internally generates `qa_sprint6_3_<32 hex>`, validates the exact prefix/name/mode, holds one dedicated PG client/transaction, checks namespace and its separate ledger before migration DDL, executes unchanged 001–003 plus proposed 004, checks schema/FKs/indexes/triggers/RLS/grants/decimals, exercises synthetic importer/API behavior and compares public metadata/data fingerprints. Exact-name cleanup is checked before DROP, then absence/public preservation after commit. No arbitrary schema CLI/environment option, wildcard cleanup or normal migration-runner change exists.

Latest certificate: `data/validation/sprint6_3-isolated-schema-certification.json`; PASS and QA schema removed. Do not install Docker/PostgreSQL, use SQLite, or substitute mocked DB behavior for this gate. UI presentation fixtures are separate from the real PostgreSQL certification.

## Continuation order

1. Protect the original 592-file baseline at `data/runtime/sprint6_3/baseline.json`. Four specifically authorized integration files differ; all remaining 588, including 6.2 artifacts and original PDFs, must still match. Never replace this baseline to conceal changes.
2. Reverify `npm run assessor:enrichment:dry-run`: original ZIP/PTS/member hashes, selected rows, exact candidate/High hashes, all situs/category/flag counts. Unexpected source reconciliation => STOP.
3. Confirm current applicable tests, UI/build/security and isolated PG certificate remain valid. Review the explicitly documented old Sprint 6.2 whole-application freeze exception; no original test was modified.
4. Recheck secure preflight/status and read-only source/score/state reconciliation. Latest Gate 9 result: TLSv1.3 authorized; 001–003 checksums match; exactly 004 pending; 11,316 unchanged candidates, 312/701/10,303 priorities, all NEEDS_ADDRESS, enrichment tables absent.
5. Gate 10 is now complete under explicit authority. The unchanged normal migration runner applied only 004 once. Its checksum matches the isolated certificate and the prior ledger rows are unchanged. The one-shot `production-gates.js` receipt prevents replay. Do not invoke it or `db:migrate` again for this continuation.
6. Gate 11 passed: two empty new tables, expected columns, ten indexes, 38 checks, three restrictive FKs, two enabled triggers matching SQL bodies, RLS/grants and preservation verified. No schema modification is needed to fix the QA harness.
7. **Gate 12 passed:** the corrected QA-only entry point uses `SELECT set_config('search_path','public,pg_catalog',true)`, verifies ledger/schema/empty tables/fingerprints before writes, then tests two exact and one unmatched synthetic candidate, rollback, replay, immutable history, next-edition supersession, null/zero, APN9/ATN, category/use, situs and unchanged v1 scores/statuses. The transaction rolls back; post-rollback batches/observations/source rows are zero/absent. No migration command was invoked.
8. **Gate 13 PASS.** Receipt `data/validation/sprint6_3-production-import.json` records all revalidated hashes/checksums, the batch ID, post-commit counts, unchanged score/status and protected-data fingerprints, privacy inventory, and provider accounting.
9. The committed batch is exactly one completed 2026 Final import: 11,316 active observations, 11,263 exact / 53 unmatched, 0 supersessions/rejections. The certified source lock, candidate locks, payload fingerprint and completion checks all passed.
10. Independent fresh-connection reconciliation passed for situs, geometry, categories, all seven flags, High 312/309/258, Residential Buildings 118, Single Family 81 (all with situs), assessment states and safe inventory.
11. Identical replay through a separate pool returned the existing completed batch with 0 batches/observations/inserts/supersessions added. Candidate scores/statuses and all existing application fingerprints are unchanged.
12. Preview deployment was certified at `6ab469b7e165d1222cdd9ad7`; the production deployment `6ab46d05cc42f49433bf22ac` then passed full-population list/filter/detail performance, 118/81 presets, server pagination, cold Function reopen, county disclaimer/privacy, confirmation gating, zero provider calls and deployed credential/log scans.
13. The first preview `6ab45d8eeff8ea741f01670e` is retained as a failed privacy receipt. Final corrective preview and production runtime are certified. **PRODUCTION CERTIFIED — KERN ASSESSOR OPPORTUNITY DISCOVERY & ENRICHMENT OPERATIONAL.** Stop before Sprint 6.4, scoring v2, KIPS, parcel-history recovery, bulk RentCast, outreach or other feature work.

## Commands / safety

`assessor:enrichment:dry-run` is offline, source-only; `status` is read-only schema presence; `qa` is source-plan unit QA; `apply` remains authorization-STOP-gated. The separately reviewed `sprint6.3:production-import` entry point performed the now-completed Gate 13 transaction and replay; do not rerun it. Real PG importer tests are part of the isolated certification entry point. Neither the UI nor its API exposes the importer.

Keep original ZIP and extracted source evidence immutable. County data exports are deferred: existing CSV remains approved PTS/canonical fields only, limited to 100 filtered rows. Keep `SPRINT6_2_SOURCE_DISCLAIMER.md` with all permitted county-derived copies/extracts. No raw/owner/contact data, full roll, KIPS/history guesses, geometry vertices, assessment-as-market-value, automatic Property creation, scoring updates or provider refresh is permitted.

Rollback means transaction rollback before commit. No destructive cleanup or production source deletion is authorized. On any discrepancy, preserve diagnostic hashes/counts, never print credentials, STOP for review. Remote Supabase certification/preflight uses real network traffic; distinguish it from zero provider calls.
