# Future Kern screening research — no scoring change

`kern-screen-v1` remains unchanged. County enrichment is contextual evidence, not a new priority score. No weights or investment-quality ranking are proposed here.

Potential future research inputs, only with reviewed provenance and missing-state treatment:

- Explicit official residential-use support, keeping zoning-only and ambiguous descriptions separate.
- Explicit vacant-use support from the county description, not inferred physical vacancy.
- County situs availability, not postal verification.
- Exact parcel-geometry availability, not surveyed boundary accuracy.
- Improvement-assessment positive, explicit zero and unknown states, without inferring current buildings or condition.

Reject: assessment = market value; assessment minus tax delinquency = equity; zero IMP_VAL = vacant land; situs = deliverable address; use code = occupancy/condition; distress = willingness to sell. No mortgage, other-lien, motivation or suitability inference.

The High residential (118) and High SFR (81) presets are implemented filters over unchanged v1 High plus certified research categories. Full source reconciliation and local presentation checks reproduce those counts; actual isolated-PG filter semantics also pass. Full-population production/preview execution awaits migration/import/deployment authority. These are not new scores.
