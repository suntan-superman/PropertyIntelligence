# Sprint 6.1 PDF QA

The fixture laboratory contains fifteen synthetic snapshots without private owner data: complete, sparse, exit conflict, many encumbrances with unknown amounts, large rehab, all 15 comps, long legal, many diligence questions, manufactured, explicit zero, no condition, historical, long sources, minimal and extreme amounts.

`npm run report:investment:test` checks source/model equality, durable-link rejection, scalar reconciliation STOPs, timestamp-independent fingerprints, source immutability, separate-service historical regeneration after a later decision/evidence, zero provider access, ID-only API behavior, HTML escaping, secret filtering and absence of Home Advisor runtime references.

`npm run report:investment:qa` renders every fixture and checks PDF.js page counts, nonempty pages, horizontal/vertical text bounds, page X/Y, all retained comp addresses, final long-content sentinels and credential patterns. Every page is rasterized without OCR; contact sheets and PDFs are under `data/validation/sprint6_1-pdf`. Complete, sparse and long fixtures also pass through the actual shared local HTTP handler with an ID-indexed synthetic repository; this is not claimed as live PostgreSQL certification. Rendering aborts all network access. The existing 351-file baseline protects preexisting reports, formulas and migrations.

Pagination: measured Letter pages, bounded fragments for long cells, repeated table headers and section continuation titles, no split table rows, text wrapping within all columns, reserved footer area. Signature MAO and primary comps are kept together when they fit. Tables and warnings have text equivalents to every chart. No provider images/maps or decorative charts.

Visual review of complete/sparse contact sheets and full-size MAO/legal continuation pages found avoidable early page breaks and a sparse trailing fragment; these were corrected by conditional section grouping, compact table spacing and removal of repeated primary valuation fields from the appendix. Long text now splits at word boundaries and repeats its field label. The final complete report is eight pages, sparse/minimal six, long legal fourteen, long sources ten and 65-item rehab thirty-four. The MAO scale/table occupy one page in the complete fixture, all fifteen comps remain in the appendix, and unknown costs/payoffs are explicitly labeled.

Performance and per-fixture results: `data/validation/sprint6_1-pdf-qa.json`. Complete fixture render approximately 0.7 seconds / 102 KB; safeguards are 30 seconds, 2 MB model input, 20 MB output and 200 pages.

Runtime limits: Netlify intentionally remains PDF-local-only. Deployed preview must certify health, model capability, historical-link STOP and ID-only API rejection, browser credentials and no provider calls. The six currently saved decisions lack Analysis Snapshot linkage; they cannot be used to claim successful historical PDF generation. No historical links are guessed or backfilled.

Actual PostgreSQL certification: `node scripts/qa-investment-postgres.js` inserted only uniquely tagged synthetic Property/Deal/Evidence/Analysis/Decision rows within a transaction, loaded/rendered the real SQL model with 15 comps, wrote report metadata plus its complete audit payload, created Decision #2 with newer evidence, and regenerated unchanged Decision #1 content. The transaction was deliberately rolled back; a fresh query proved the synthetic Property absent. No committed QA records or updates to existing records. This supplements the repository-double API tests without claiming separate committed-connection certification. Results: `data/validation/sprint6_1-postgres-qa.json`.

Preview `6ab061f7588dbc7d7fb59f6b`: PostgreSQL health, model capability, current renderer limitation, historical-link STOP, ID-only request and invalid-ID rejection passed. Browser asset scan passed. Built Function ZIP scan passed (276,974 bytes, no browser renderer or Home Advisor runtime dependency). Sanitized preview Function logs: command exit 0, nine output lines, zero credential/provider-request patterns. No raw logs retained or reported. No RentCast calls.
